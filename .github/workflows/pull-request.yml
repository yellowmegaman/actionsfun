on:
  push:
    branches:
    - master

jobs:
  pull-request:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: pull-request
      uses: yellowmegaman/prtrigger@v0.1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
