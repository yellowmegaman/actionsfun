name: push flow
on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '*.*' 
jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@master
      - name: build project
        run: |
          echo "linter1"
          echo "linter2"
